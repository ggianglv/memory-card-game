version: 2.1
orbs:
  node: circleci/node@1.1
jobs:
  build:
    executor:
      name: node/default
      tag: '12.8'
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: node --version
            - run: npm install
            - run: npm run build